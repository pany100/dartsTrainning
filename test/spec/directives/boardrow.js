'use strict';

describe('Directive: boardRow', function () {

  // load the directive's module
  beforeEach(module('dartTrainningApp'));

  var element,
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

});
